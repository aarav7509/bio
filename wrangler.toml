name = "newsletter-subscribe"
main = "worker.js"
compatibility_date = "2026-01-01"

# Created by `wrangler kv namespace create SUBSCRIBERS` — paste the id it prints below.
kv_namespaces = [
  { binding = "SUBSCRIBERS", id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID" }
]
